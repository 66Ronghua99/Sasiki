{
  "name": "@sasiki/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node runtime for Sasiki migration (pi-mono + Playwright MCP)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@mariozechner/pi-agent": "^0.9.0",
    "@mariozechner/pi-ai": "^0.55.4",
    "@modelcontextprotocol/sdk": "^1.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
