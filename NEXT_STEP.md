在当前仓库内已本地化的 `artifacts/e2e/20260308_110124_276` 样本上，继续把 `sop-compact-hitl` 从最小 file-based CLI 升级为 question-driven 入口：直接读取 `execution_guide.detailContext.unresolvedQuestions` / `clarification_questions.json` 生成澄清顺序，用户无需手写 field 名就能完成一轮 compact-stage HITL，然后回写 `intent_resolution.json` 并可选 rerun；完成后更新 checklist，并通过 `npm --prefix apps/agent-runtime run typecheck` 与 `build`。
