进入 `SOP Compact V1` 全链路切换阶段：先把 `clarification_questions` 迁移为 agent-owned 并建立 `semantic_intent_draft.blockingUncertainties -> clarification_questions` 的 coverage gate，再冻结 `execution_guide.v1` 的 `generalPlan + detailContext` schema，最终将 replay 主编译入口切到 V1；完成后更新 checklist，并通过 `npm --prefix apps/agent-runtime run typecheck` 与 `build`。
