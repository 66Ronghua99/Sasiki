在已清理 legacy 输出的 `SOP Compact V1` 主链路上进入下一步验收：为 `run_id=20260308_110124_276` 补一条最小 `intent_resolution.json` 样本，验证 `semantic_intent_draft + clarification_questions + intent_resolution -> execution_guide.v1(replayReady=true)` 的 ready-path；同时冻结 `semantic field -> resolution field -> replay gate` 的映射，完成后更新 checklist，并通过 `npm --prefix apps/agent-runtime run typecheck` 与 `build`。
